<Window x:ConnectionId='1'
    x:Class="WallpaperSwitcher.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WallpaperSwitcher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Grid x:ConnectionId='2' x:Name="RootGrid" Padding="16,16,16,8" Background="Transparent" CornerRadius="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Title Bar Area -->
            <RowDefinition Height="*" />    <!-- Grid Area -->
            <RowDefinition Height="Auto" /> <!-- Pagination/Settings Area -->
        </Grid.RowDefinitions>

        <!-- Title Bar Area -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <!-- WP Badge -->
            <Border Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" CornerRadius="6" Padding="6,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="WP" FontSize="11" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
            </Border>
            
            <!-- Custom Close Button -->
            <Button x:ConnectionId='16' HorizontalAlignment="Right" VerticalAlignment="Center" 
                    Background="Transparent" BorderThickness="0" Padding="8" CornerRadius="6"
                                           ToolTipService.ToolTip="Close">
                <FontIcon Glyph="&#xE711;" FontSize="12" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
            </Button>
            
            <!-- Toast Notification -->
            <Border x:ConnectionId='17' x:Name="ToastNotification" 
                    Background="{ThemeResource SystemAccentColor}" 
                    CornerRadius="6" 
                    Padding="12,6" 
                    HorizontalAlignment="Center" 
                    Opacity="0"
                    IsHitTestVisible="False">
                <TextBlock x:ConnectionId='18' x:Name="ToastText" Foreground="White" FontSize="12" FontWeight="Medium" />
            </Border>
        </Grid>

        <!-- Grid layout for images (3x2) -->
        <GridView x:ConnectionId='3' Grid.Row="1"
                  x:Name="WallpaperGridView"
                                                         
                                            
                  IsItemClickEnabled="True"
                  SelectionMode="None"
                  HorizontalAlignment="Center"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid MaximumRowsOrColumns="2" Orientation="Horizontal" HorizontalAlignment="Center" />
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>

            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid Width="160" Height="90">
                        <Border CornerRadius="8" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                            <Image Source="{Binding FilePath}" Stretch="UniformToFill" />
                        </Border>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
            
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="Margin" Value="4" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="CornerRadius" Value="8" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="GridViewItem">
                                <Grid>
                                    <!-- Using standard ContentPresenter instead of strict ListViewItemPresenter -->
                                    <ContentPresenter 
                                        ContentTransitions="{TemplateBinding ContentTransitions}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Margin="{TemplateBinding Padding}" />
                                    
                                    <!-- Custom Blue Hover Border -->
                                    <Border BorderBrush="#0078D7" 
                                            BorderThickness="2" 
                                            CornerRadius="10" 
                                            Opacity="0"
                                            Margin="-2"
                                            x:Name="HoverBorder"
                                            IsHitTestVisible="False"/>
                                            
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal" />
                                            <VisualState x:Name="PointerOver">
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.1" />
                                                </Storyboard>
                                            </VisualState>
                                            <VisualState x:Name="Pressed">
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity" To="0.5" Duration="0:0:0.1" />
                                                </Storyboard>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </GridView.ItemContainerStyle>
        </GridView>

        <!-- Empty State UI (Brilliant Aesthetic) -->
        <StackPanel x:ConnectionId='4' Grid.Row="1" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center" 
                                                                                     
                    Spacing="12"
                    Opacity="0.7">
            <FontIcon Glyph="&#xEB9F;" FontSize="42" Foreground="{ThemeResource TextFillColorPrimaryBrush}" HorizontalAlignment="Center"/>
            <TextBlock Text="No wallpapers found" FontSize="16" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" HorizontalAlignment="Center" />
            <TextBlock Text="Click the gear icon below to pick a folder." FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" />
        </StackPanel>

        <!-- Bottom Area -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <!-- Settings / Configure Folder Button -->
            <Button x:ConnectionId='5' HorizontalAlignment="Stretch" VerticalAlignment="Center"
                    Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" 
                    BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}" 
                    BorderThickness="1" CornerRadius="6" Padding="12,8"
                                             >
                <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                    <FontIcon x:ConnectionId='6'                                                    FontSize="14" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                    <TextBlock x:ConnectionId='7'                                                   FontSize="13" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Window>

