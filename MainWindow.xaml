<Window
    x:Class="WallpaperSwitcher.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WallpaperSwitcher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" RequestedTheme="Light" Padding="16,16,16,8" Background="Transparent" CornerRadius="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Title Bar Area -->
            <RowDefinition Height="*" />    <!-- Grid Area -->
            <RowDefinition Height="Auto" /> <!-- Pagination/Settings Area -->
        </Grid.RowDefinitions>

        <!-- Title Bar Area -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <!-- WP Badge -->
            <Border Background="#4A4D54" CornerRadius="6" Padding="6,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="WP" FontSize="11" FontWeight="Bold" Foreground="White" />
            </Border>
            
            <!-- Toast Notification -->
            <Border x:Name="ToastNotification" 
                    Background="{ThemeResource SystemAccentColor}" 
                    CornerRadius="6" 
                    Padding="12,6" 
                    HorizontalAlignment="Center" 
                    Opacity="0"
                    IsHitTestVisible="False">
                <TextBlock x:Name="ToastText" Foreground="White" FontSize="12" FontWeight="Medium" />
            </Border>
        </Grid>

        <!-- Grid layout for images (3x2) -->
        <GridView Grid.Row="1"
                  x:Name="WallpaperGridView"
                  ItemClick="OnImageClicked"
                  IsItemClickEnabled="True"
                  SelectionMode="None"
                  HorizontalAlignment="Center"
                  ScrollViewer.VerticalScrollBarVisibility="Hidden">
            
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid MaximumRowsOrColumns="3" Orientation="Horizontal" HorizontalAlignment="Center" />
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>

            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid Width="90" Height="90">
                        <Border CornerRadius="8" Background="#1AFFFFFF">
                            <Image Source="{Binding FilePath}" Stretch="UniformToFill" />
                        </Border>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
            
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="Margin" Value="4" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="CornerRadius" Value="8" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="GridViewItem">
                                <Grid>
                                    <!-- Using standard ContentPresenter instead of strict ListViewItemPresenter -->
                                    <ContentPresenter 
                                        ContentTransitions="{TemplateBinding ContentTransitions}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Margin="{TemplateBinding Padding}" />
                                    
                                    <!-- Custom Blue Hover Border -->
                                    <Border BorderBrush="#0078D7" 
                                            BorderThickness="2" 
                                            CornerRadius="10" 
                                            Opacity="0"
                                            Margin="-2"
                                            x:Name="HoverBorder"
                                            IsHitTestVisible="False"/>
                                            
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal" />
                                            <VisualState x:Name="PointerOver">
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.1" />
                                                </Storyboard>
                                            </VisualState>
                                            <VisualState x:Name="Pressed">
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity" To="0.5" Duration="0:0:0.1" />
                                                </Storyboard>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </GridView.ItemContainerStyle>
        </GridView>

        <!-- Bottom Area -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Pagination Buttons -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Content="&lt;" Click="OnPreviousClicked" HorizontalAlignment="Stretch" Margin="0,0,4,0" 
                        Background="#F0FFFFFF" BorderBrush="#1A000000" BorderThickness="1" CornerRadius="6" Foreground="#333333" FontWeight="Bold" />
                <Button Grid.Column="1" Content="&gt;" Click="OnNextClicked" HorizontalAlignment="Stretch" Margin="4,0,0,0" 
                        Background="#F0FFFFFF" BorderBrush="#1A000000" BorderThickness="1" CornerRadius="6" Foreground="#333333" FontWeight="Bold" />
            </Grid>
            
            <!-- Settings Icon -->
            <Button Grid.Row="1" HorizontalAlignment="Right" Margin="0,8,-8,-4" 
                    Background="Transparent" BorderThickness="0" Padding="4">
                <FontIcon Glyph="&#xE713;" FontSize="14" Foreground="#FF666666" />
            </Button>
        </Grid>
    </Grid>
</Window>
